# Supabase Configuration
SUPABASE_URL=your-project-url.supabase.co
# Publishable Key (formerly "anon key")
# - Safe to use in the browser
# - Respects Row Level Security (RLS)
# - Used by the Angular app
SUPABASE_ANON_KEY=your-anon-key-here
# Secret Key (formerly "service_role key")
# - NEVER use in the browser/client
# - BYPASSES all RLS policies (full admin access)
#   - Only used for:
#     - Server-side operations
#     - Edge Functions
#     - Admin tasks
#     - Migrations (CLI handles this automatically)
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# Auth Provider Configuration (optional)
SUPABASE_AUTH_GOOGLE_CLIENT_ID=your-google-client-id
SUPABASE_AUTH_GOOGLE_SECRET=your-google-secret
SUPABASE_AUTH_APPLE_CLIENT_ID=your-apple-client-id
SUPABASE_AUTH_APPLE_SECRET=your-apple-secret

# Anthropic Claude API (for Edge Functions)
ANTHROPIC_API_KEY=your-anthropic-api-key
