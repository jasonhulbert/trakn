export const environment = {
  production: true,
  supabaseUrl: '', // Will be set during deployment or via environment variables
  supabaseAnonKey: '' // Will be set during deployment or via environment variables
};
